[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "retail-structure-qualification"
version = "0.1.0"
description = "Public verification artifacts for unified_rerun_v1"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
