# Contributing

Do not add raw data, processed panels, credentials, prediction arrays, checkpoints, caches, logs, or third-party source copies. Contributions must preserve `unified_rerun_v1` provenance and pass artifact-hash verification. Discuss any portable rerun implementation before coding it because it requires a new execution record.
