# Data directory

Keep downloaded source files under `data/raw/` and locally generated panels under `data/derived/`. Both locations are ignored by Git. This repository contains neither raw data nor a portable v1 preprocessing command.
